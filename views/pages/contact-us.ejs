<%- include ../partials/header %> <%- include ../partials/categories-navbar %>

<div class="container">
  <div class="row">
    <div class="col-lg-8 col-md-12 m-auto">
      <h1 class="mt-4 mb-4 text-center">Kontakt</h1>
      <!-- Flash message -->
      <% if (successMsg != null && successMsg) { %>
        <div id="success" class="alert alert-success">
          <%= successMsg %>
        </div>
        <% } %>
    </div>
    <div class="col-lg-8 col-md-12 text-justify m-auto">
      <!-- Introduction -->
    <div class="mt-4 mb-4">
      <p>Kdykoli se na nás můžete obrátit! Neváhejte nám napsat e-mail nebo zavolat na níže uvedené kontaktní údaje.
        Můžete nás také navštívit nebo nám zavolat kdykoli během naší pracovní doby od <i>pondělí do pátku: 9:00 až 17:00.</i> <br />
        Na e-maily odpovídáme i o víkendech. Proto nás neváhejte kontaktovat.
      </p>
    </div>
      <!-- Email contacts -->
      <div class="mt-4 mb-4">
        <p>
          <h6><i>Kontakt:</i></h6>
        </p>
        <p>
          Pro problémy s objednávkami:
          <a href="mailto:orders@email.com">
            <i class="fa fa-envelope" aria-hidden="true"></i>
            <u>orders@email.com</u>
          </a>
        </p>
        <p>
          Technická podpora:
          <a href="mailto:support@email.com">
            <i class="fa fa-envelope" aria-hidden="true"></i>
            <u>support@email.com</u>
          </a>
        </p>
      </div>
      <!-- Phone contacts -->
      <div class="mt-4 mb-4">
        <p>
          <h6><i>Telefon:</i></h6>
        </p>
        <p>
          Centrála:
          <a href="tel:555-555-5555">
            <i class="fa fa-phone" aria-hidden="true"></i>
            555-555-5555
           </a>
        </p>
        <p>
          Majitel firmy:
          <a href="tel:722-729-854">
            <i class="fa fa-phone" aria-hidden="true"></i>
            722-729-854
           </a>
        </p>
        <p>
          Whatsapp:
          <a href="tel:722-729-854">
            <i class="fa fa-phone" aria-hidden="true"></i>
            722-729-854
           </a>
        </p>
      </div>
    </div>

    <!-- Contact us form -->
    <div class="col-lg-8 col-md-12 m-auto">
      <div class="mt-5 mb-4">
        <h2>Kontakt</h2>
        <!-- Error flash message -->
        <% if(errorMsg!=null && errorMsg.length>0) {%>
          <div id="flash-msg" class="alert alert-danger">
            <% errorMsg.forEach((message) => { %>
            <p>
              <%=message %>
            </p>
            <%}) %>
          </div>
          <%}%>
        <form action="/pages/contact-us" method="POST">
          <div class="form-group">
            <label for="name">Jméno</label>
            <input type="name" class="form-control" name="name" id="name" required />
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input type="email" class="form-control" name="email" id="email" required />
          </div>
          <div class="form-group">
            <label for="message">Obsah</label>
            <textarea class="form-control" name="message" id="message" rows="5" placeholder="Zde napište zprávu..." required></textarea>
          </div>
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <button type="submit" class="btn btn-primary button-style btn-contact">
            Odeslat
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<%- include ../partials/footer %>
